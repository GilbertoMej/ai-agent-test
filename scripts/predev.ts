import "dotenv/config";
// scripts/predev.ts — runs once before `next dev` + worker boot.
// Stub for D-20 (auto-embed on dev startup). The real embed loop ships in 01-06.
console.log("predev: stub — embed-tool-docs ships in 01-06 (Phase 1 Wave 2)");
